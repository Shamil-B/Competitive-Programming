def insertionSort1(n, arr):
    # Write your code here
    j = 0
    for item in arr:
        arr[j] = str(item)
        j+=1

    num = arr[-1]
    for i in range(n-2,-2,-1):
        if i==-1:
                arr[0] = num
                print(" ".join(arr))
        elif int(arr[i])<int(num):
            arr[i+1] = num
            print(" ".join(arr))
            break

        else:
            n = arr[i]
            arr[i+1] = n
            print(" ".join(arr))

    
